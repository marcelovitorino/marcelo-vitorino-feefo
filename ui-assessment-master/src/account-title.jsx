import React from 'react';
import styled from 'styled-components';
import './account-title.css'; // Import the CSS file

const AccountTitleContainer = styled.div;

const AccountTitle = () => {
  return <AccountTitleContainer className="AccountTitleContainer">Account Overview</AccountTitleContainer>;
};

export default AccountTitle;
